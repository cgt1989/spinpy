# Entorno completo para ejecutar la interfaz (visor.py).
#
# El paquete `spinpy` por si solo no necesita ni Qt ni el mallador: si solo
# vas a importarlo desde un script o un cuaderno, basta con el primer bloque.
# La separacion por extras esta en pyproject.toml; este archivo es el atajo
# para montar el entorno de la aplicacion de una sola orden:
#
#     pip install -r requirements.txt

# --- nucleo ---------------------------------------------------------------
numpy>=1.24
scipy>=1.10
scikit-image>=0.21
pyvista>=0.42

# --- solver ---------------------------------------------------------------
# pyamg no es opcional de verdad: sin el, la homogeneizacion y el ensayo caen
# a CG con Jacobi, que con el contraste 1e-6 entre hueso y vacio NO converge.
pyamg>=4.2

# --- mallado TET10 para exportar solidos ----------------------------------
pymeshfix>=0.16
tetgen>=0.6

# --- lote y estadistica ---------------------------------------------------
pandas>=2.0
joblib>=1.3

# --- interfaz grafica -----------------------------------------------------
# OJO CON LA LICENCIA: PyQt5 es GPL v3 o comercial. Este codigo es MIT, pero
# quien redistribuya la aplicacion junto con PyQt5 queda sujeto a la GPL sobre
# la obra combinada. Ver LICENSE. Migrar a PySide6 (LGPL) esta pendiente.
PyQt5>=5.15
pyvistaqt>=0.11
matplotlib>=3.7
